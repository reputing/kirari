# Copy to .env.local and fill in from your Supabase project settings → API.
# Leave BOTH blank to run fully on localStorage (no backend) — the public page
# will still show your saved edits on the same browser.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
